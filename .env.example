# ================================================
# PRODUCTION (Vercel Environment Variables)
# ================================================
# Set these in Vercel Dashboard > Settings > Environment Variables
# These are SERVER-SIDE only and never exposed to the browser.

# Neon Database Connection
# Get your connection string from Neon Console: https://console.neon.tech
NEON_DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# ================================================
# LOCAL DEVELOPMENT ONLY
# ================================================
# These VITE_ prefixed vars are only for local development.
# They are NOT needed in production (Vercel uses the server-side vars above).

# Direct DB connection for local dev (optional - uses API proxy if not set)
VITE_NEON_DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# Development Mode
# Set to 'true' to use localStorage demo mode (no database needed)
VITE_DEV_MODE=false

# Google Drive Integration (Optional)
# Setup instructions:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project (or select existing)
# 3. Enable "Google Picker API" and "Google Drive API"
# 4. Create OAuth 2.0 Client ID (Web application type)
#    - Add authorized JavaScript origins: http://localhost:5173 (dev), https://yourdomain.com (prod)
# 5. Create API Key
VITE_GOOGLE_API_KEY=your-api-key-here
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
